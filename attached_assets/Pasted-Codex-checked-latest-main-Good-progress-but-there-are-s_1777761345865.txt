Codex checked latest main. Good progress, but there are still code-level issues. Please fix before calling this complete.

Critical issue #1 — Town prop render/data mismatch
In artifacts/trencheria/src/game/components/TownDistrict.tsx:

You moved TOWN_PROPS collision/validator data:
- prop at [20,48]
- prop at [24,48]
- cart at [28,48]

But MarketSquare() still renders the actual visible objects at the old positions:
- <MarketStall pos={[20, y, 58]} ... />
- <MarketStall pos={[24, y, 58]} ... />
- <Cart pos={[28, y, 58]} ... />

This means RailwayValidator can report clean, but the visible game scene can still overlap the bridge. Please make rendered props and TOWN_PROPS use the same single source of truth, or update both positions together.

Acceptance:
- No hardcoded rendered market prop remains at the old bridge-overlap position if the validator data moved it.
- Validator and visual scene must match.

Critical issue #2 — Rivermoor ford is validator-only / unclear visually
BridgeData.ts now adds INTENTIONAL_FORDS and suppresses the Rivermoor road-water warning. But I do not see a dedicated rendered ford/quay/causeway at ford-rivermoor-quay [432,332]. RiverTown has docks around local z=-30, which becomes about world z=320, but the intentional ford is centered around z=332. That is not clearly the same visual feature.

Please either:
- add a visible shallow ford/quay/causeway mesh exactly around ford-rivermoor-quay, or
- move the ford definition to match an actually rendered quay/dock feature,
- and document it in code.

Acceptance:
- The validator should not silently suppress water warnings unless the player can actually see the intended ford/quay in-world.

Critical issue #3 — Visual proof is still blocked
The Playwright screenshot could not reach the actual Ironhold view because the game stuck at 84% loading. Build passing and validator passing are not enough for this visual bug.

Please either:
- fix the 84% loading hang, or
- add a dev-only camera/test route that can render Ironhold without full login/loading,
- then capture fresh screenshots from the same main-kingdom angle.

Acceptance:
- Fresh screenshot proves the crossed bridge / box railway issue is gone.
- Screenshot must show Ironhold front gate, market area, bridge area, and railway.

Do not touch SQL, Supabase, auth, api-server, migrations, or .migration-backup.

Final acceptance:
- Validator clean.
- Rendered scene and validator data match.
- Rivermoor ford is visibly rendered if it is validator-approved.
- Ironhold screenshot proves no bridge/rail/building intersections.
- Build passes.
