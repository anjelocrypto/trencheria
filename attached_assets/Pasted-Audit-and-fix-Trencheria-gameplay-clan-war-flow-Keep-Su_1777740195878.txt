Audit and fix Trencheria gameplay/clan-war flow. Keep Supabase + monorepo architecture unchanged. Do not delete users, wallets, clans, clan_members, player_accounts, territories, or war history.

Focus areas:
1. Faction spawning
- Files: artifacts/trencheria/src/game/systems/FactionData.ts, SafeSpawn.ts, Player.tsx
- Verify every registered wallet user spawns at their faction kingdom using session.faction_id.
- Verify all 7 faction spawn points are safe: not water, not inside buildings/props/collision, not outside playable terrain.
- Fix bridge/water validation if bridges are incorrectly rejected as water.
- If Yetis/Dogs are meant to be playable, make their availability consistent. If not, prevent selection clearly.

2. Clan war countdown logic
- Files: artifacts/trencheria/src/game/hooks/useClanSystem.ts, WarNotifications.tsx, WarScoreboard.tsx
- Ensure transition_war_states runs automatically from the frontend at a safe cadence, because there is no Supabase scheduler in repo.
- Pending wars must become active when countdown ends.
- Active wars must become pending_resolution when battle ends.
- UI countdowns must refresh smoothly and not get stuck at “Now”.

3. PvP battle rules
- Files: artifacts/trencheria/src/game/components/Player.tsx, artifacts/trencheria/src/game/GameScene.tsx
- Audit whether players can damage other factions outside active wars.
- Fix so faction PvP damage only works when:
  - there is an active territory challenge between the two factions
  - both players are inside the active war territory radius
  - same faction players cannot damage each other
  - guests/no-faction players cannot PvP
- Also validate incoming PvP hit broadcasts client-side before applying damage.

4. War start/cancel UI
- Files: artifacts/trencheria/src/game/ui/FactionPanel.tsx, useClanSystem.ts
- The faction panel currently shows kingdoms/wars but must expose clear actions:
  - challenge enemy kingdom when eligible
  - cancel pending outgoing challenge when eligible
  - show disabled reason when not eligible
- Keep UI beginner-friendly and avoid breaking existing layout.

5. SQL/gameplay permissions
- Files: supabase/pending/new SQL file
- Audit challenge_territory and cancel_challenge.
- Current fixed factions register players as member, but war RPCs may require leader. That can block all real users from starting clan wars.
- Create a safe pending SQL migration, transaction-wrapped, that preserves all existing data.
- Do not delete or rewrite current users.
- If changing rules, allow verified fixed-faction members to start/cancel faction wars, or clearly explain safer alternative.
- Include post-apply verification queries.

6. Verification
- Run api-server typecheck.
- Run trencheria typecheck and identify pre-existing errors vs new errors.
- Run build if possible.
- Smoke-test manually or describe exact manual tests:
  - new wallet registration
  - existing wallet login
  - faction spawn per kingdom
  - challenge territory
  - countdown pending → active
  - PvP only inside active war zone
  - kill logging/scoreboard
  - active → pending_resolution

Return:
- Summary
- Critical issues fixed
- Files changed
- SQL file created
- Remaining risks/manual tests
