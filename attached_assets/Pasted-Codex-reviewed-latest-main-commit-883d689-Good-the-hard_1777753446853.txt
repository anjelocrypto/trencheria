Codex reviewed latest main commit 883d689.

Good: the hardening fixes are mostly correct:
- RailwayData distToRailway cache bug is fixed.
- LevelCrossings now uses lc.size.
- stray attached_assets prompt file was deleted.
- opengraph.jpg was restored.
- RailwayValidator now checks stations, resources, wilderness buildings, roads, rails, and bridges.

But this is NOT fully complete yet because the validator still reports 8 world-map violations:
- 5 uneven station/platform footprint issues
- 2 station/platform water issues
- 1 station/platform road-overlap issue

Please do not call this complete until RailwayValidator reports zero world-map violations.

Do not touch SQL, Supabase, auth, supabase/pending, or .migration-backup.

Next task:
1. Run the game in dev.
2. Copy the exact RailwayValidator warnings from the browser console.
3. Fix every reported station/map issue in the actual map data, not by hiding the warnings.
4. Likely files:
   - artifacts/trencheria/src/game/world/RailwayData.ts
   - artifacts/trencheria/src/game/world/RegionData.ts
   - artifacts/trencheria/src/game/world/WaterData.ts
   - artifacts/trencheria/src/game/world/BridgeData.ts
   - artifacts/trencheria/src/game/components/RailwayStations.tsx only if station layout math is wrong
5. For uneven stations: move station/platform to flatter terrain or add intentional station pad flattening.
6. For water-touching stations: move station/river/lake, or add an intentional dock/causeway and teach the validator that it is intentional.
7. For road-overlap station: reroute the road, move the station platform, or add an intentional station forecourt/crossing and validate it explicitly.
8. Improve the road-overlap check if needed so it uses exact rotated platform footprint vs road segment, not only rough center distance.

Final acceptance:
- Browser console must show RailwayValidator success with 0 world-map violations.
- Typecheck/build should pass or only show known unrelated pre-existing errors.
- Manual preview around every station: Ironhold, Thornwall, Greenmeadow, Goldenvale, Blackthorn, Rivermoor, Darkhollow.
